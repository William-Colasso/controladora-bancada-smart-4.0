# Copie para .env (mesma pasta do pom.xml) e ajuste para o seu ambiente.
# O application.properties importa este arquivo via spring.config.import (opcional);
# sem .env, valem os defaults (localhost:3306 / root / senai / porta 8088).

DB_URL=jdbc:mysql://localhost:3306/DB_SMART_4_0
DB_USERNAME=root
DB_PASSWORD=senai
SERVER_PORT=8088
